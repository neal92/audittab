# Webhook URL exposed by your n8n or AI endpoint
VITE_AI_WEBHOOK_URL=https://n8n.example.com/webhook/generate_stucture

# Token used by your webhook (if required). For dev only. Prefer server-side storage for production.
VITE_AI_TOKEN=Bearer YOUR_TOKEN_HERE
